const nomes = ['Cristhian', 'Maryana', 'Marcela', 'Max', 'Matheus'];

// nomes.splice(indice, qtd.delete, add.element1, add.element2, add.element3);

const removidos = nomes.splice(-1, 1);

console.log(nomes, removidos);
