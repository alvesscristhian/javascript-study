const nome = ['Cristhian', 'Maryana', 'Maria', 'Cleiton'];

for (let valor of nome) { // Usar quando precisar dos valores mesmo, sem ser indices ou chaves
    console.log(valor);
}


// For Classico - Geralmente com iteraveis (array ou strings)
// For in - Retorna o indice ou chave (strings, arrays ou objetos)
// For of - Retorna o valor em si (iteraveis, array ou string)